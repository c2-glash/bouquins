{% extends 'base.html.twig' %}

{% block title %}Ajouter un auteur{% endblock %}
{% set headerh1 = 'Ajouter un auteur' %}
{% set pageType = 'GestionCompte' %}

{% block body %}

{% include './layout/header.html.twig' %}
    <main>
        <section class="container">
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success">
                        {{ message }}
                    </div>
                {% endfor %}

                {{ form_start(auteur_form) }}
                    {{ form_widget(auteur_form) }}
                    <input type="submit" class="btn btn-primary" value="Ajouter un auteur">
                {{ form_end(auteur_form) }}
        </section>

        <section class="container">
            <a href="/ajout/livre" class="btn btn-primary">Ajouter un livre</a>
            <a href="/ajout/categorie" class="btn btn-primary">Ajouter une catégorie</a>
        </section>
    </main>
{% include './layout/footer.html.twig' %}
{% endblock %}
